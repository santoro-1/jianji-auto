# 2026-08-31 更新、打包与其他电脑部署

本说明区分 **Windows 剪映处理工作台** 和 **RunningHub 中央网站**。推送 Git 不等于部署；以下部署命令由操作者在确认目标后执行。

## 本批更新范围

- H3 费用预览后续修复：恢复稳定行 ID，确认前核对素材版本，部分选行/旧预览失效时显式撤销整个未提交预览后重算；必须先更新云端安全撤销接口，再重新打包工作台。无数据库迁移，不自动部署、撤销或重新付费生成；之前已打好的 ZIP 不包含本次源码修改。

- H3：本地 ASR 定位正常发音，在不改变音画时长的前提下清理片头杂音；失败可免费重试本地清理，不自动重新付费生成。
- H3：剪映草稿主轨使用独立的生成片段；浏览器仍可播放合并预览。保留原始文件、清理后的权威音轨和片段版本绑定，防止误用旧片段。
- 后续源码修复：过长的本地素材路径自动复制为新草稿内的短路径引用，解决独立片段“暂无访问权限”。需重新打包并重新生成后期草稿；原始视频可直接复用，不重跑 H3，旧草稿不自动改写。
- 音频后续修复：显式重新生成（含仅调速）统一创建幂等新批次，解决 H3 已审核声音重试409；云端接收前保留原音视频与字幕，拒绝后不再卡生成中。退出后按原请求只读查回 v1 声音操作，提交不明时提示核对并阻止盲目重提。需先更新云端再重新打包工作台，不覆盖用户运行数据或自动改写旧版无合同记录。
- BGM：按实际播放区间计算响度，统一预览与草稿增益，并增加淡入、峰值及短时响度约束。
- 语义素材：分类文件夹图库、增量索引、食物细分类与候选排序。2026-09-01 后续源码已将新构建工作台的正式启动默认改为文件夹模式并自动建“素材”目录；不自动迁移线上旧素材库。2026-08-31 已生成的更新 ZIP 仍是旧默认，必须重新构建才生效。
- RunningHub 网站本批还包含 H3 远端 PNG 缺失的安全人工重试、声音预检与视频账号配置解耦，以及声音原请求回执的只读查询接口；不只是文档更新。此前余额保护、下载、账号路由也需确认已部署。

不把 LTX/统一启动器仓库另存的一整批未提交改动混进本次发布。本说明交付的是 `JianyingRenderServer` 工作台，不是新的 LTX 全家桶。

本轮源码验证：数字人服务器全量 `578 passed`；工作台相关扩展回归 `246 passed`，另有页面异常刷新补测 `2 passed`。
均为隔离数据和模拟供应商调用，未实际部署或付费生成。现有 2026-08-31 16:06 的工作台更新 ZIP 早于本轮源码，必须重新打包后使用。

## 1. 现有电脑更新：先在开发机打代码包

当前开发机已经有构建环境。在 PowerShell 执行：

```powershell
Set-Location -LiteralPath 'D:\工作内容\轻盈健\公寓\jyd_plain_json_probe'
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build\build_processor.ps1 -Clean -UpdateOnly
if ($LASTEXITCODE -ne 0) { throw '构建失败，不要发包' }
Get-FileHash -Algorithm SHA256 -LiteralPath .\release\JianyingRenderServer-update-windows-x64.zip
```

产物：`release\JianyingRenderServer-update-windows-x64.zip`。

包括 EXE、完整 `_internal` 依赖、前端和草稿工具；**不含 `data`、ASR、公共素材或用户项目**。已有 ASR 的电脑可直接用此包；没有本地 ASR 的电脑还需要下面第 3 节的运行时。

不要用旧日期的 `update-*-code.zip` 代替本次产物。后者来自另外的统一启动器仓库，内容和安装目标不同。

## 2. 在目标电脑安装代码更新包

先等正在运行的生成/导出结束，退出工作台和统一启动器。保持自己的账号配置、`data`、素材和 ASR 不动。将 ZIP 复制到目标电脑后执行下面整段：

```powershell
$ErrorActionPreference = 'Stop'
$updateZip = (Resolve-Path -LiteralPath (Read-Host '更新 ZIP 的完整路径，不加引号')).Path
$target = (Resolve-Path -LiteralPath (Read-Host '含 JianyingRenderServer.exe 的安装目录，不加引号')).Path
if (-not (Test-Path -LiteralPath (Join-Path $target 'JianyingRenderServer.exe'))) {
    throw '目录不对：必须直接包含 JianyingRenderServer.exe'
}
if (Get-Process JianyingRenderServer,PublicVideoWorkbenchLauncher,WorkbenchServiceManager -ErrorAction SilentlyContinue) {
    throw '请先退出工作台及统一启动器；不要在运行中覆盖'
}
$updateTemp = Join-Path $env:TEMP ('jyd-update-' + [guid]::NewGuid().ToString('N'))
New-Item -ItemType Directory -Path $updateTemp | Out-Null
Expand-Archive -LiteralPath $updateZip -DestinationPath $updateTemp
if (-not (Test-Path -LiteralPath (Join-Path $updateTemp 'JianyingRenderServer.exe'))) {
    throw '不是本说明对应的扁平工作台更新包'
}
foreach ($forbidden in @('data','asr_runtime')) {
    if (Test-Path -LiteralPath (Join-Path $updateTemp $forbidden)) {
        throw "该包包含 $forbidden，不可按代码更新方式覆盖"
    }
}
$updateEntries = @(Get-ChildItem -LiteralPath $updateTemp -Force)
$codeBackup = Join-Path (Split-Path -Parent $target) ('jyd-code-backup-' + (Get-Date -Format 'yyyyMMdd-HHmmss'))
New-Item -ItemType Directory -Path $codeBackup | Out-Null
foreach ($entry in $updateEntries) {
    $oldEntry = Join-Path $target $entry.Name
    if (Test-Path -LiteralPath $oldEntry) {
        Copy-Item -LiteralPath $oldEntry -Destination $codeBackup -Recurse -Force
    }
}
foreach ($entry in $updateEntries) {
    Copy-Item -LiteralPath $entry.FullName -Destination $target -Recurse -Force
}
Write-Host "代码更新完成，旧代码备份：$codeBackup"
```

**统一安装目录要选择其中的 `digital-human` 子目录**，不能选择含启动器的外层目录。独立安装则选择 EXE 所在目录。只复制 ZIP 内同名代码，不删除旧目录，不覆盖 `data`。

然后按原来的入口启动，浏览器按 `Ctrl+F5`。已有统一安装继续用原统一启动器，不需要换 LTX。

如果更新失败，保持程序关闭，先保留更新后的 `data`，将上述备份中的旧代码复制回原目录再检查；不要用其他电脑的数据库覆盖自己的数据库。

## 3. 在开发机打新电脑完整包（含 ASR、FFmpeg、公共素材）

完整包面向新的空安装目录，**不要拿完整包覆盖老电脑**。需要构建 Python、`vendor\jy-draftc`、已配置好的公共素材库，以及 RunningHub 媒体节点 Python 环境和模型缓存。当前开发机已有这些前置环境。

下面所有步骤在同一个 PowerShell 窗口顺序执行。默认打成供局域网共用的处理机，例如 250；只供本机使用时，将 `shared` 改为 `standalone`。

### 3.1 先生成不带真实令牌的便携 ASR

```powershell
$ErrorActionPreference = 'Stop'
$jyd = 'D:\工作内容\轻盈健\公寓\jyd_plain_json_probe'
$rh = 'D:\工作内容\轻盈健\数字人\runninghub_mvp'
$releaseStamp = Get-Date -Format 'yyyyMMdd-HHmmss'
$asrOutput = Join-Path $rh ('dist\jyd-asr-' + $releaseStamp)
powershell -NoProfile -ExecutionPolicy Bypass -File (Join-Path $rh 'media_node\build-portable-media-node.ps1') -OutputRoot $asrOutput -KeepExpanded
if ($LASTEXITCODE -ne 0) { throw 'ASR 便携包构建失败' }
$asrStages = @(Get-ChildItem -LiteralPath $asrOutput -Force -Directory | Where-Object { $_.Name -like '.rh-media-full-*.stage' })
if ($asrStages.Count -ne 1) { throw '未找到唯一的 ASR 打包目录' }
$asrBundle = $asrStages[0].FullName
foreach ($required in @('python\python.exe','media_node\asr_service\app.py','ffmpeg\bin\ffmpeg.exe','ffmpeg\bin\ffprobe.exe')) {
    if (-not (Test-Path -LiteralPath (Join-Path $asrBundle $required))) { throw "缺少 $required" }
}
if (-not (Test-Path -LiteralPath (Join-Path $asrBundle 'media_node\.runtime\models'))) {
    throw '缺少 ASR 模型缓存，先准备模型再打可离线使用的包'
}
```

不要添加 `-IncludeLocalConfig`，否则可能把当前服务器令牌带到别人的电脑。这里仅借用便携环境；JYD 启动的是本机 ASR，不需要运行媒体节点 Worker，不需要复制开发机的 `.env`。

如果旧电脑只缺 ASR：把这里完整的 `$asrBundle` 目录内容复制到它的 `JianyingRenderServer.exe` 同级 `asr_runtime` **空目录**，并把其中 `ffmpeg` 目录复制到 EXE 同级。保留旧 `data`，检查自己配置的 ASR 地址是否为 `http://127.0.0.1:18084`。不要覆盖正在使用的已有运行时。

### 3.2 打工作台，加入 FFmpeg，并生成完整 ZIP

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File (Join-Path $jyd 'scripts\build\build_processor.ps1') -Clean -DeploymentMode shared -AsrBundleRoot $asrBundle -SkipArchive
if ($LASTEXITCODE -ne 0) { throw '工作台完整构建失败' }
$fullDist = Join-Path (Split-Path -Parent $jyd) '.jyd-build-cache\dist\JianyingRenderServer'
Copy-Item -LiteralPath (Join-Path $asrBundle 'ffmpeg') -Destination $fullDist -Recurse -Force
$fullZip = Join-Path $jyd ('release\JianyingRenderServer-full-asr-' + $releaseStamp + '.zip')
$archivePython = Join-Path $rh 'media_node\.runtime\venv\Scripts\python.exe'
& $archivePython (Join-Path $rh 'media_node\create_portable_zip.py') --flat $fullDist $fullZip
if ($LASTEXITCODE -ne 0) { throw '完整 ZIP 打包失败' }
Get-FileHash -Algorithm SHA256 -LiteralPath $fullZip
Write-Host "完整安装包：$fullZip"
```

这里使用现有 ZIP64 打包工具，以兼容模型/素材大文件。完整包包含批准用于分发的 `data\libraries`、母版库、素材管理配置和新的处理机配置示例；不会复制 `control.db`、用户上传、用户任务与输出。但公共素材可能较大，发包前仍要确认分发权和文件内容。

`runtime\test_environment` 文件夹图库不在包内。测试图库的代码会发布，测试数据不会随代码发布。不要直接压缩整个开发目录发给别人。

## 4. 在新电脑安装完整包

Windows x64；先装好兼容的剪映并正常打开一次。导出需要可用的 Windows 桌面会话。目标电脑不需要另外安装 Python。

```powershell
$ErrorActionPreference = 'Stop'
$fullZipOnTarget = (Resolve-Path -LiteralPath (Read-Host '完整 ZIP 的路径，不加引号')).Path
$newInstall = 'D:\JYD-Processor'
if (Test-Path -LiteralPath $newInstall) { throw '请选择不存在的新安装目录，禁止覆盖旧安装' }
New-Item -ItemType Directory -Path $newInstall | Out-Null
Expand-Archive -LiteralPath $fullZipOnTarget -DestinationPath $newInstall
$env:PATH = (Join-Path $newInstall 'ffmpeg\bin') + ';' + $env:PATH
Start-Process -FilePath (Join-Path $newInstall 'JianyingRenderServer.exe') -WorkingDirectory $newInstall -WindowStyle Hidden
```

需要更改安装位置时，修改 `$newInstall`。再次启动可在 PowerShell 设置 `$newInstall` 后重跑最后两行；这个 PATH 只影响本次启动及子进程，不修改机器的全局环境变量。

网站：`http://127.0.0.1:8010/app/new`。共享模式下，其他电脑访问 `http://处理机实际局域网IP:8010/app/new`；通过 Windows 防火墙允许可信私有网络访问 8010，不把 ASR 的 18084 暴露到公网。

首次使用检查 `data\processor_config.json` 中的数字人服务器地址、剪映草稿路径和部署模式，然后用自己的账号登录。此工作台包不包含单独的 LTX 服务；有 LTX 需求需另行安装。

### 验收（已有电脑更新后同样执行）

```powershell
Invoke-RestMethod -Uri 'http://127.0.0.1:8010/api/health' -TimeoutSec 15
Invoke-RestMethod -Uri 'http://127.0.0.1:18084/healthz' -TimeoutSec 15
```

ASR 健康检查只证明进程已启动；`modelLoaded=false` 表示模型还没加载。首次交付还应做一次真正的本地识别：

```powershell
$sampleWav = (Resolve-Path -LiteralPath (Read-Host '用于本地 ASR 验收的短 WAV 路径，不加引号')).Path
curl.exe --fail --show-error --noproxy '*' -F "audio=@$sampleWav" 'http://127.0.0.1:18084/v1/transcribe'
if ($LASTEXITCODE -ne 0) { throw '本地识别失败，请查看 data\logs\asr.log' }
```

这是本地转写，不会付费生成 H3。若缺失模型，首次识别可能需要联网下载；要离线交付，必须事先确认缓存完整并测试成功。已有 ASR 配置了令牌的环境应沿用本机配置，不要在群聊或文档粘贴真实令牌。

再检查已有任务的片段预览、原始片段下载与本地清理状态；新生成一次草稿后应在主视频轨看到多个独立片段。旧剪映草稿不会被更新包主动改写。浏览器的合并预览不是“独立片段功能没生效”。

## 5. 中央 RunningHub 网站更新（和 Windows 工作台是两件事）

服务器当前的实际部署版本需要部署脚本检查，不能仅凭 Git 已推送判断。本批有服务器代码和新增只读声音查询接口，必须按下列流程更新并重启对应服务，再更新 Windows 工作台。新旧两端兼容：旧工作台仍可使用新服务器；新工作台连接未升级服务器时保留待核对警告，不盲目重提。无新增依赖或数据库迁移。

在有 Git 和部署 SSH 权限的开发机执行，不是在服务器直接 `git pull`：

```powershell
Set-Location -LiteralPath 'D:\工作内容\轻盈健\数字人\runninghub_mvp'
git status --short
# 工作区必须干净，再同步 main；若有本地修改，停止，不要强制覆盖。
git switch main
if ($LASTEXITCODE -ne 0) { throw '切换 main 失败' }
git pull --ff-only origin main
if ($LASTEXITCODE -ne 0) { throw '同步失败' }
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\deploy-code-update.ps1
if ($LASTEXITCODE -ne 0) { throw '发布前检查失败，不要继续' }
```

上一步只做测试和服务器只读检查。核对显示的服务器、路径和 commit 无误后，再由操作者明确执行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy\deploy-code-update.ps1 -Deploy
```

保留脚本的测试、排空和确认步骤，不加 `-SkipTests`。如果脚本报告依赖/迁移等不属于代码更新范围，停止并按 `runninghub_mvp/deploy/README.md` 走完整部署，不能强行覆盖。没有部署密钥的新电脑不要复制他人的私钥，改由授权机器执行。
