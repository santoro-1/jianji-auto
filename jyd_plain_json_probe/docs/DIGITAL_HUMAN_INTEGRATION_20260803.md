# 数字人任务接入剪映工作台变更记录（2026-08-03）

## 状态

本轮工作台改造已在本地代码完成并通过测试，尚未部署。普通用户账号改为复用数字人网站账号；工作台本身只保留素材、机器和运行设置所需的技术管理员。

> 2026-08-10 清晰化补充：数字人网站已在每个数字人分段后增加一对一 SeedVR2 48G
> 放大。工作台增加 `VIDEO_ENHANCING` 活动状态，主下载取得清晰片段，并在本地素材元数据
> 保存 `quality_variant=seedvr2_upscaled`；工作台不会再次调用 SeedVR2，也不会默认下载云端
> 保留的数字人源片段。完整 mock 回归为数字人 `288 passed`、工作台 `369 passed`；经用户
> 授权的云端 5 秒真实冒烟已确认清晰片段为 1080×1920、节点 `97/mp4`。

## 连接方式

- 工作台服务端口改为 `8010`，避免与数字人网站的 `8000` 冲突。
- 源码联调可通过环境变量连接 `http://127.0.0.1:8000`；交付给用户的 Windows 工作台
  默认连接正式数字人网站 `https://video.lanyingjk01.com`。
- 配置字段统一为 `data/processor_config.json` 中的 `digital_human_server_url`。
- Windows 工作台旧配置中的 `http://127.0.0.1:8000`、`http://192.168.11.28:8000`
  或旧云端认证地址会迁移到正式数字人网站；用户明确填写的其他 HTTPS 数字人地址不会被覆盖。
- 正式上线时只需把 `digital_human_server_url` 改为云端数字人网站 HTTPS 地址，无需迁移云端现有账号。

## 登录与管理员边界

- 普通用户在工作台输入账号密码后，由数字人网站完成验证；工作台不保存密码，只保存短期会话令牌。
- 工作台 `/admin` 不再作为普通账号管理入口，而是跳转数字人网站 `/admin`。
- 工作台独立技术管理入口保留为 `/local-admin/login`，用于素材、机器和本地运行设置。
- 数字人账号被停用、删除或修改密码后，现有工作台会话失效。

## 数字人任务收件箱

工作台新增“数字人任务”区域，每 15 秒刷新当前登录账号自己的任务。刷新只更新列表，不会自动下载、自动导入、自动渲染或自动发布。

| 数字人任务 | 工作台动作 |
| --- | --- |
| `AUTO_READY` 且 `AUTO_POSTPROCESS` | 显示“一键导入工作台”，下载唯一完整视频并载入精确字幕时间戳 |
| 上传音频任务 | 显示需要人工处理，并提供原始片段下载 |
| 文本语音被切成多个片段 | 显示需要人工处理，并提供每个原始片段下载 |

自动导入成功后会切换到视频生成页面、选中本地视频源并填入字幕。操作人仍需选择 BGM、字幕字体/样式、保存目录并确认渲染。

## 工作台新增接口

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/digital-human/tasks` | 拉取当前登录账号的数字人任务 |
| `POST` | `/api/digital-human/tasks/{item_id}/import` | 导入唯一的自动后期视频及精确字幕 |
| `GET` | `/api/digital-human/tasks/{item_id}/videos/{video_index}` | 下载人工处理任务的指定原始片段 |

上述接口使用工作台当前会话，并由服务端携带短期令牌访问数字人网站，不把令牌暴露给前端下载地址。

## 字幕规则

- 渲染接口支持精确字幕 cue JSON 和 SRT，能够保留数字人网站返回的开始、结束时间。
- 字幕预览使用同一套校验逻辑；重叠、逆序或无效时间段会拒绝。
- 视频尾部与最后一句字幕只存在不超过 1 秒的小误差时，会裁到视频结尾；更大的不一致会报错，避免静默错位。
- 用户编辑字幕文字或更换视频源后，已导入的精确 cue 会失效，工作台按新内容重新处理。
- 上传音频或多片段任务建议先人工粗剪，再使用剪映字幕识别和人工校对。

## 本地文件与保存目录

- 从数字人网站导入的视频作为本地媒体引用保存，不上传阿里云。
- 输出目录继续由操作人在本机选择，并使用浏览器本地存储记住上一次选择。
- 本轮未增加自动发布；完成的本地视频仍按原工作台流程交给后续服务。

## 验证结果

- 工作台完整自动化测试：`183 passed`。
- 另有 30 条依赖库的既有 deprecation warnings，没有新增测试失败。
- 数字人网站配套测试：`156 passed`，另有 1 条既有 warning。
- 已检查 Python 语法和 Git diff 格式。
- 测试过程中没有修改生产数字人账号或数据库。

## 本地联调步骤

1. 先启动数字人网站，确认监听 `http://127.0.0.1:8000`。
2. 再运行 `start_processor.ps1`，打开 `http://127.0.0.1:8010/app`。
3. 使用同一套本地数字人测试账号登录工作台。
4. 打开“数字人任务”，确认任务列表在 15 秒内出现。
5. 用单片段文本任务测试一键导入和字幕预览。
6. 用多片段文本任务、上传音频任务测试逐段下载。
7. 选择本地输出目录完成一次渲染，确认目录选择被记住。

## 尚未执行

- 未构建或发布新的生产安装包。
- 未将工作台连接到正式云端数字人网站。
- 未执行自动发布或阿里云上传。

## 2026-08-04 新版模块 3 补充

- `/app/new/voices` 已复用数字人网站原有声音克隆、融合、试听和保存能力，不建立第二套
  声音资产或 MiniMax 配置。
- 核心工作台已通过 `/api/new/voices*` 和 `/api/new/projects/*/audio*` 接入官方音色、
  逐行音色、批量声音生成、状态同步、试听、下载和单行重试。
- 项目默认音色由工作台后端原子地应用到全部脚本行，单行接口仍可覆盖；声音中心明确为
  “生成试听、试听确认、保存到音色库”三步，未保存的制作结果不会进入工作台选择列表。
- 保存后的自定义音色需要用户显式点击“激活”，由数字人后端完成第一次正式 TTS 并切换
  `ACTIVE` 后才能供核心工作台选择；自定义音色卡支持二次确认删除，项目仍引用时拒绝。
- 声音中心支持粘贴外部克隆工具取得的 MiniMax `voice_id`。数字人后端先用当前账号的 Key
  查询 `voice_cloning` 列表核对归属；导入不执行 TTS。用户确认该 ID 已在同一账号成功调用
  过 T2A 时直接登记为 `ACTIVE`，否则登记为 `READY` 并保留显式激活步骤。
- 工作台保存 `digital_human_audio_batch`、`digital_human_audio_item` 关联、音频素材历史
  版本和 MiniMax 原始字幕时间戳；项目状态和按钮权限由后端返回。
- 音频批次固定停在数字人后端 `AWAITING_REVIEW`，本模块不会自动进入 RunningHub。
- 声音生成只提交脚本、音色和语音参数，不上传图片；用于远程批次的内部幂等键采用固定
  长度哈希，避免项目编号和音色编号拼接后超过数字人数据库字段长度。
- 核心工作台默认声音区新增 `0.5–2.0` 生成语速偏好（快捷值 `0.8×/0.9×/1.0×`）。该
  用户级偏好跨音色沿用，新批次和单行重新生成都会传给数字人后端；已有音频不变。
- 自动化测试全部 mock MiniMax，没有产生真实费用；没有部署服务器或修改云端账号数据库。

## 2026-08-04 新版模块 4A 补充

- 工作台新增项目画面启动、真实状态轮询、单行失败阶段重试和基础视频下载接口；浏览器
  仍只访问工作台后端，账号令牌只由工作台服务端代理。
- 用户明确确认费用后，工作台才让数字人后端现有音频审核任务进入 RunningHub；没有
  建立第三套后端、账号、任务队列或 RunningHub 调用实现。
- 工作台在这个 4A 动作中才上传脚本行的当前图片，并由数字人后端绑定到已审核音频；
  图片不会提前参与模块 3 的声音任务。
- 每个 SeedVR2 成功清晰分段都下载为不可覆盖的 `original_video_segment`；该素材角色名
  表示工作台时间轴的原始有序分段，metadata 会注明清晰化来源。单/多片段均
  生成按已确认音频时长标准化的 `base_video`，多片段按顺序自动拼接并使用 0.25 秒
  保时长叠化。
- `base_video` 不等于最终 `composition_video`。4A 页面禁用字幕、BGM 和变体操作，基础
  视频完成后不会自动创建变体。
- 音频完成后、4A 启动前仍允许替换当前图片；4A 上传并绑定最后一次选择的图片。

## 2026-08-04 新版模块 4B 补充

- 4B 完全位于工作台后端；没有增加数字人接口、第三套后端、账号或渲染队列，浏览器仍
  只调用工作台 `/api/new/*`。普通预览不启动剪映，明确下载时才复用现有
  `RenderJobQueue` 和剪映处理机按需导出一次。
- MiniMax 原始 cues 永久保留；派生 render cues 固定居中单行、最大宽度 80%、中心距底部
  约 20%，并按用户选择的真实字体文件测量字宽，禁止自动换行。无法安全排版时进入
  `REVIEW_REQUIRED`，不创建预览或剪映任务。
- 可选 BGM 来自工作台现有音乐库；程序测量人声/曲目响度。普通音乐目标为低于口播
  11 dB，音量冻结在 `0.08..0.25`，失败回退 `0.18`；强人声音乐允许参与匹配，但改用
  低于口播 15 dB、`0.05..0.16` 的范围，失败回退 `0.1136`。浏览器预览与剪映导出共用该值；字幕
  按 render cues 随音频内容动态变化。BGM 从成片结尾向前铺设，最后一轮落到曲目自然结尾，
  不足时向前循环并以 0.2 秒交叉衔接；浏览器预览、普通导出与变体共用。只有明确下载才登记
  新的 `composition_video`。
- 后续变体直接使用原始分段/上传视频和同一字幕/BGM 配方，在变体任务中一次剪映导出；
  不先导出普通成片，因此不会重复控制剪映。自动多片段普通成片和变体把每段分别写入
  剪映主轨，静音片段原声并加入完整 MiniMax 音轨；每段精确占用原计划区间，片段略短时
  只轻微调整该段画面速度。片段间使用剪映原生“叠化”，4B 不重新触发 RunningHub，完成后
  不自动生成变体。

## 2026-08-04 新版模块 5 补充

- 视频预览固定为 9:16，播放后中央按钮隐藏；浏览器仍只访问工作台接口。
- 单个 RunningHub 原始片段直接下载，多片段由工作台按原顺序打包 ZIP 并附顺序清单，
  不再调用数字人后端重新生成或下载已经落盘的片段。
- 上传本地视频会创建新的工作台 `composition_video` 版本并设为当前视频，自动视频和全部
  原始片段继续保留。用户上传文件视为已完成视频，原 MiniMax 字幕标记失效，不自动叠加。
- 完整测试结果：数字人 `162 passed`，工作台 `207 passed`；均未调用真实付费接口，
  未部署生产环境，未修改云端账号数据库。

## 2026-08-05 模块 6、7 补充

- 变体仍由工作台冻结基础/上传视频及模块 4B 配方后一次提交剪映，不回调数字人后端。
- 最终变体默认直接写入 `D:\auto\月.日\批次号`，原始脚本表格先复制到同一目录；成果库
  依据工作台数据库中的账号、项目、源视频版本和剪映批次索引展示，并核对实际文件。
- 模块 6 和成果库的变体预览固定为 9:16；本轮没有调用 MiniMax/RunningHub、没有部署生产
  环境，也没有修改云端账号数据库。
- 工作台完整自动化测试为 `217 passed`。

## 2026-08-05 交互与默认值修正

- 新版 4A 的时间戳音频先把各段小数时长向上取整到整秒，不再追加旧入口的音频静音尾垫；
  仅整个任务最后一个 RunningHub 分段的 `end_time` 再加 1 秒用于表情收尾，工作台保留该秒
  画面。数字人后端旧版上传音频算法保持不变。
- 模块 4B 默认字体改为素材库真实字体 `DouyinSansBold`，历史项目已保存字体不覆盖。
- 成果库提供总全选、批次全选和批次缩略入口；点击批次后才查看该批次全部 9:16 成果。
- 核心工作台脚本列限制宽度，长文本自动断行并在单元格内纵向滚动。
- 完整自动化回归为数字人 `163 passed`、工作台 `217 passed`；未执行真实付费任务或
  剪映 GUI 导出，未部署生产环境。

## 2026-08-06 智能内容分析模块 5

- 工作台新增逐 `ProjectItem` 内容分析快照，调用数字人后端既有
  `POST /api/workbench/content-analysis`；浏览器和工作台仍不取得 Ark Key。
- Excel/CSV 保存和脚本编辑不自动请求分析。点击“生成声音预览”后，工作台才对本批声音
  目标中首次导入或文本变化的行发起请求；每次数字人请求只包含一条完整脚本，项目批量最多
  并发 10 行，各行、音乐分支和字幕分支分别保存成功或失败。
- 脚本 SHA-256 与快照绑定。修改一行只把该行声音与分析置为待重做；下次生成声音时才处理
  该行。文本未变而重新生成声音不会重做分析；单行显式分析重试使用强制刷新，且不得覆盖
  此前已成功的分支。
- 分析失败是安全降级：不调用 MiniMax、RunningHub 或剪映，不清空 MiniMax `raw_cues`，
  不修改当前音频、基础视频、成片或变体状态。
- 本模块没有接入字幕字符到 MiniMax 时间轴的映射，也没有执行本地音乐 Top1；二者分别
  留给后续智能内容分析模块 6、7。本轮自动化只使用 mock，未调用真实付费服务或部署生产。

## 2026-08-06 智能内容分析模块 6

- 工作台已在 4B 本地预览阶段把合法 `subtitle_units` 映射到 MiniMax `raw_cues`，数字人
  服务端仍不生成字幕时间戳，接口也没有新增模型时间字段。
- 新 MiniMax 音频保存脚本 SHA-256/长度。只有当前脚本、内容分析快照、当前音频和 raw cues
  绑定四方一致时使用语义断点；音频重生成但文本未变时复用分析并重做本地映射。
- 空格/换行允许被 MiniMax cue 省略，其余字符必须逐字一致；连接词、数字、专有词和关联
  单元按 `bind`/`break_after` 形成不可拆组，`~` 必须精确匹配。
- 映射或版本校验失败时继续原有 raw cues/真实字宽排版，不覆盖 raw cues，不重试豆包、
  MiniMax、RunningHub 或剪映。本模块没有接入音乐 Top1。

## 2026-08-06 智能内容分析模块 7

- `project_music.py` 在 4B 本地阶段读取合法 `music_intent` 和当前音频真实时长，从 46 首
  已审核素材中逐行返回唯一 Top1；项目批量按已使用次数施加确定性重复惩罚，使同类文案在
  分数接近的合格曲目间适度轮换，不重新请求数字人服务端或任何付费接口。
- 自动选择快照绑定脚本摘要、当前音频、时长、matcher/taxonomy/profile 版本和标签库哈希。
  音频变化但脚本未变时保留内容分析、失效旧 AI 选择，并在下次 4B 按新时长重算。
- 音乐分支与字幕分支继续独立。音乐分析或匹配失败时使用项目明确默认音乐，否则无 BGM；
  手动曲目和手动无 BGM 均优先，只有用户选择“AI 智能匹配”才恢复自动模式。
- 最终 BGM 写入既有 4B 配方，变体冻结继承，不作为变体维度。本模块定向回归 `40 passed`，
  工作台完整 mock 回归 `260 passed`。

## 2026-08-06 智能内容分析模块 8

- 数字人项目新增跨项目契约测试，将实际 `analyze_content` 响应直接传入本工作台响应复核、
  语义字幕映射和本地音乐匹配器，避免两个项目只验证各自手写 mock。
- 字幕排版不再把“整行放得下”当作唯一的不切分条件。普通云端 `prefer` 仍是软偏好；明确的
  提示语/问题/评价到答案，以及较长编号项由本地提升为强语义边界。13em/80% 仍是硬上限，
  词内、数字内、结构助词前和黏连后缀内部仍不可切分。
- 验收覆盖双成功、音乐成功/字幕失败、字幕成功/音乐失败、错误索引安全重算，以及空格、
  换行和 `~` 精确处理；同时复核并发 10、逐行版本隔离、失败回退和手动 BGM 优先级。
- 跨项目测试 `3 passed`；最终完整 mock 回归为数字人 `216 passed`、工作台 `260 passed`，
  0 failure、0 error。未调用真实豆包、MiniMax、RunningHub 或剪映，未部署生产环境。

## 2026-08-08 多片段剪映时间轴修正

- 数字人 4A `base_video` 继续用于浏览器预览；工作台 4B/变体导出改为把原始视频分段分别
  写入剪映主轨，而不是把 `base_video` 当作单个片段。
- 每段按原计划目标时长定位，短片段通过剪映片段速度轻微补齐，原声全部静音；完整 MiniMax
  音频作为唯一权威语音轨从 0 开始，但在导出时钳制到正文主视频结束，避免 MP3 编码尾巴
  多撑几十毫秒；字幕继续使用原始绝对时间戳，避免误差逐段累计。
- 两个真实 MP4 之间仍写入 0.25 秒剪映原生叠化，不创建尾帧图片片段。

## 2026-08-08 画面状态轮询稳定性修正

- 工作台 4A 状态同步只查询仍为 `PENDING/RUNNING` 的画面操作，不再每次轮询重复访问已经
  成功或失败的历史任务，避免项目行数增加后放大云端请求量。
- 数字人网站普通 API 请求默认超时由 4 秒提高为 15 秒；连续连接失败时前端只提示一次，
  随后最长退避到 30 秒自动重试。状态查询失败不会重新提交 RunningHub 付费任务；连接恢复
  后继续同步、下载基础视频并进入后续本地处理。

## 2026-08-08 换图重生成与小数范围字幕修正

- 4A 操作快照记录当前输入图片素材 ID 和 SHA-256，调用云端时一并提交图片摘要；接收分段
  或基础视频前再次核对云端摘要，避免换图后把同一远程条目的旧视频登记为新版本。
- 云端对同图请求保持幂等；图片字节变化时只重做 RunningHub 画面阶段，已审核 MiniMax
  音频、原始 cue 和 ASR 对齐输入继续复用，不产生新的语音合成费用。
- 本地语义排版将含单位的数值范围视为不可拆表达式。即使模型在 `0.` 与 `5到1公斤` 之间
  给出断点，最终字幕仍保留 `0.5到1公斤`，不会把小数点误当成隐藏标点。

## 2026-08-07 语义前景图片集成

- 工作台从独立受控目录召回字符候选，数字人网站新增独立视觉契约/API/缓存，只返回语境
  决策；具体素材、MiniMax 时间映射、密度、安全区和人工锁定全部保留在本地。
- “每天吃一个鸡蛋”可生成鸡蛋建议；成语“鸡蛋里挑骨头”、否定“这不是鸡蛋”和词语引用
  默认跳过。浏览器预览与 4B 剪映轨道消费同一冻结配方。

## 2026-08-09 短句提前贴图与固定人名牌

- 自动语义图从关键词所在短句的开头出现，并至少保持到关键词说完后 400ms；“比如早上喝碗
  杂粮粥”会从“比如”开始显示，不再等到“杂粮粥”三个字。
- 黄瓜自动素材改为真实照片的画面下方三分之一渐变版本。未人工锁定的旧项目自动配方会读取当前素材
  默认布局，人工锁定项不改。
- 张雒人名牌已复制到受控素材库，浏览器预览、4B 成片和后续变体均在封面 3 帧后从正文
  第 1 帧显示到结束；轨道
  从上到下为字幕、人名牌、语义前景图片、基础画面/特效，不新增审核列。
- 视觉分支可独立失败和重试，不触发语音、RunningHub 或剪映重生成。自动化验收全部使用
  mock；真实 Ark 质量抽样、剪映 GUI 导出和生产发布仍需明确授权。
- 本轮最终完整 mock 回归为数字人网站 `272 passed`、工作台 `299 passed`。

## 2026-08-10 人名板、字幕与顶部固定标题

- 固定人名板改为草稿 `jyd_eab56dad6e7e` 中启用的新版 PNG，并锁定同一草稿的缩放和位置参数；
  浏览器预览、普通成片与变体统一使用受控素材库，不引用桌面测试路径。
- 字幕统一为 14 号、1080×1920 参考 Y=-850；安全宽度仍为 80%，单行容量由旧 13em 调整为
  约 10.21em，断句仍以真实字体字宽计算。
- 历史 `postprocess.top_title` 的 `label`/`headline` 两字段继续兼容保存；正文视频现统一改为
  单行固定“世界冠军带你自律”，字号 19、Y=1535、红字白描边，浏览器预览、剪映导出与变体
  共用同一冻结参数。模型两行标题只继续用于封面。
- `postprocess.cover_title` 新增两行短标题，每行最多 5 字。封面从模块 6 手工变体参数上移到项目
  后处理配方，底图固定使用当前上传人物图而非视频截图；普通成片和全部变体继承同一 3 帧封面。
  大模型只规划生成标题文字，字体、颜色、位置、阴影和黑框继续由本地固定代码控制。

## 2026-08-09 统一内容与视觉分析

- 工作台仍先在本地召回 catalog 候选，但候选以无路径、无时间的 `visual_context` 合并进
  既有 `/api/workbench/content-analysis`；一次 Ark 请求同时产生音乐意图、字幕断点和
  selected-only `visual_plan`。
- 云端只决定合法 anchor、concept 和 priority。具体图片、raw cues 时间绑定、密度调度、
  人工锁定和剪映写入继续位于工作台独立模块。
- 项目原视觉分析入口已改为复用统一内容分析协调器，不再调用数字人网站独立视觉接口。
  MiniMax 时间轴晚于内容分析返回时，工作台保留语义计划，音频同步后只做本地重新绑定，
  不产生第二次模型调用。

## 2026-08-10 统一图片/视频占用与原生视频轨道

- 工作台自动配方改为 `mixed`：静态概念优先图片，运动动作优先视频，缺少目标媒体时在本地
  回退；图片和视频共享一张占用表。这里记录的是 2026-08-10 的初版分句实现；时间下限、
  asset 去重和 enrichment 密度已经由下方 2026-08-13 `sentence-v1` 章节取代。
- `video_overlay_apply.py` 把视频直接写成剪映原生 video material/segment，支持截取、静音、
  循环、fit、小窗和全屏；浏览器通过鉴权内容接口播放同一冻结配方。
- 轨道固定为 `下方图片/小窗视频 < 固定人名牌 < 全屏 B-roll < 字幕`。全屏 B-roll 只依赖
  层级自然遮住人名牌，不维护人名牌显隐事件。
- 默认受控库现有 191 张图片和 19 条视频。首批人工审片导入时，原有胯下击掌和腹部核心
  视频通过文件哈希去重，只合并概念与标签；带 `相关素材/b-roll/enrichment` 标签的素材用于
  通用空窗（当前至少 6 秒）或 v2 接缝兼容，普通动作素材用于明确语义。两类素材仍只参与
  同一次统一内容分析。
- 食物、运动和无适合素材脚本均有统一调用回归；无视觉候选时音乐和字幕仍分析一次，视觉计划
  与本地配方保持为空，不会为了数量添加无关素材。

## 2026-08-13 句段编排、成片去重与拼接点空镜

- 云端协议不变：同一次内容分析仍只返回 `anchor_id/concept_id/priority`，没有新增模型调用；
  句段、速切点、素材 ID、循环和数字人接缝全部由工作台确定。
- 普通素材从关键词所在句段开头显示到句段结束，短句保底 2 秒并受成片结尾截断；顿号列举在
  完整句段内按关键词语音顺序无缝速切，单项不强制 2 秒。
- 每条最终成片维护 `used_asset_ids`。手工锁定项先占用，自动选材遇到已用或文件无效的候选会
  尝试同概念下一个素材；全部用尽则跳过，不在 20 秒后复用同一素材。20 秒同概念限制仍作为
  密度控制保留。
- 工作台从当前数字人原始分段的 `start_seconds/end_seconds/script_text` 取得接缝。接缝按下一段
  开头语境独立尝试获准的未用视频空镜；没有对应空镜时不改变原 `video_sequence` 和溶解。
- 通用空镜由 `VISUAL_BROLL_TARGET_INTERVAL_SECONDS=10` 控制，约每 10 秒在附近寻找一个确有
  本地素材支撑的相关短句，实际空窗最短 6 秒；匹配不到不强行添加。占位优先级为手工锁定、
  接缝空镜、通用全屏空镜、明确语义。选材优先级为精确动作/对象、同类场景或分类回退、
  编辑型空镜池；精确视频存在时不采用宽泛编辑池。视频源不足时按剩余可用时长自然提前结束，
  不循环或定格补齐。
- 冻结 recipe v2 增加可选 `timing_policy_version=sentence-v1`、`used_asset_ids` 和句段/列举/接缝
  元数据；历史 recipe v1/v2 缺少这些字段时仍能预览和导出。

## 2026-08-13 图片精确、视频分级召回（本地已实现，未部署生产）

- 工作台视频素材语义拆成 L1 领域、L2 类别、L3 精确对象/动作，并额外保存动作和场景事实。
  L1 只用于管理；图片只把 L3 精确概念发送为可选候选，不允许类别兜底。
- 视频候选按 L3 精确优先；普通空镜或数字人片段接缝缺少精确视频时，才允许使用人工批准的
  L2、动作或场景回退。没有合格视频时，本地保持既有数字人分段和溶解，不要求云端硬选。
- 数字人网站仍只在一次内容分析中从工作台提供的合法 concept 中做语境选择，不取得素材路径，
  也不自行推导父子概念。具体层级回退、素材 ID、全视频去重、循环和接缝占位仍由本地完成。
- `nutrition.protein` 等抽象营养概念不得通过层级扩展成鱼、肉、鸡蛋等具体图片或视频；只有直接
  表达抽象概念的知识素材可以命中。该边界同时适用于新版完整合同和旧云端兼容回退。
- 本地 catalog 现有 451 条视频带 `video_taxonomy`，927 张图片无该字段；136 条视频具有明确
  `fallback_concept_ids`。食物、菜品、饮品的 L2 只用于视频归档，不允许同类替换。迁移不改变
  云端接口或生产数据库，当前也没有部署到生产环境。

## 2026-08-10 分辨率变更后的 SeedVR2 补跑

- 已成功生成数字人但没有 SeedVR2 记录的旧云端行，修改项目分辨率后不再重新调用数字人。
  工作台复用原 `digital_human_audio_item` 的 `remote_item_id`，调用云端显式补跑接口，并把
  `COMPOSITION_GENERATE` 操作标记为 `scope=seedvr2_backfill_only`。
- 补跑阶段不上传人物图片，不修改云端已锁定的数字人分辨率，也不产生新的数字人任务；每个
  已保存数字人 MP4 只新增自己的一次 SeedVR2 48G 清晰化。快速拼接和本地主下载继续等待并
  消费放大结果。
- 云端先验证整行所有源片段再统一入队，重复请求幂等。任一源片段不存在或未成功时返回明确
  冲突，不把放大前视频登记为新基础视频；SeedVR2 已失败时重试仍只复用保存的源片段。
- 项目分辨率是下一次画面生成的输入设置。修改它不会再清空已经完成行的 `base_video`、
  `composition_video` 或页面下载入口；只有用户真正重新生成某一行时，才按该行生成流程创建
  新版本并切换当前指针。这样历史 SeedVR2 结果不会因为来回切换 1280/1920 而从页面消失。
- 单条“下载此视频”仅在存在 `composition_video` 时直接下载；只有 SeedVR2 `base_video` 时
  必须调用本地后处理导出并等待剪映成片。一键音频和一键视频按“当前已完成子集”打包，未完成
  行会被跳过并提示数量，避免一条失败任务让整批下载不可用。

## 2026-08-11 RunningHub 手动取消后的分阶段重新生成

- RunningHub 手动取消代表原外部命令已终止。再次点击“生成视频”会创建新的外部任务，而不是
  复用被取消的任务 ID；业务行和本地操作历史仍保留，便于审计。
- 数字人阶段取消后，工作台提交项目当前分辨率，云端使用保存的图片和音频重新创建数字人任务；
  SeedVR2 阶段取消后，云端复用已落盘数字人视频，只重新创建 SeedVR2 48G 任务。
- 阶段由云端任务/增强记录判断，项目当前值 `1024` 仅是本次数字人输入值，不是特殊修复条件。
- 云端拒绝重试时，本地新操作立即标为失败，不再留下假的排队状态。本改动需要数字人网站和
  本地工作台同时更新；无需修改生产数据库。

## 2026-08-11 管理员多 RunningHub 账号资源池与 4A 后台启动

- 只有统一数字人账号摘要为 `is_admin=true` 时，首次 4A 费用弹窗才读取并展示获授权的
  RunningHub 账号摘要；每次打开重新默认全选，至少选择一个。普通用户继续自己的单账号。
- 三个首次入口（单行、选中行、全项目）只提交内部账号 ID。retry 和 SeedVR2 backfill 不
  重新选号，继续使用云端不可变快照和原流水线账号。
- 4A POST 只持久化逐行操作后快速返回。图片上传和云端启动由 4 个受控后台线程完成，每行
  使用稳定幂等键、图片资产 ID/SHA-256、分辨率及账号快照；单行失败不阻断其余行。
- `STARTING` 只表示本地进程已认领，`RUNNING` 才表示云端已接受。进程重启把 STARTING 恢复
  为 PENDING，下一次已登录轮询使用原幂等键续跑；登录令牌不落库、不写日志。
- 工作台不推算 RunningHub 5×N 容量，也不调用 SeedVR2。每个数字人分段和对应放大是否锁定
  同一真实账号、远程恢复与模糊提交保护均由云端负责。

## 2026-08-12 独立数字人池与 SeedVR2 池

- 2026-08-11 的同账号链路冻结为 `same_account_v1` 并在云端开关默认关闭时保持不变。开关开启
  且用户具有不可变 ID授权时，安全摘要返回 `dual_pool_v1` 和数字人/SeedVR2 两组账号列表。
- 费用确认分别渲染两组复选框，每次重新默认全选且每组至少一个。首次启动把执行模式和两组
  内部 ID写入本地逐行操作；工作台不向云端传一个可绕过授权的开关，也不取得任何凭据配置。
- 后台协调和重启恢复使用冻结快照。云端可以让 B 执行数字人、C 执行 SeedVR2；普通失败、
  明确账号不可用换号、取消、下载、恢复、看门狗和模糊提交容量保护全部由云端尝试绑定负责。
- `Cx_ceshi` 是否获得受控测试能力由云端用户 ID授权决定，本地不通过用户名硬编码放权。
- 4A 费用确认会明确显示当前“一控多/双池”和候选账号；启动后项目行按云端权威
  `execution_assignments` 显示实际执行账号。一控多显示每个分段的“数字人 + SeedVR2”同账号，
  双池分别显示数字人和放大账号，尚未预留时显示“待分配”。该安全摘要随逐行操作持久化，
  刷新或工作台重启后仍可恢复，且不包含任何 RunningHub 凭据或工作流配置。
- 候选账号安全摘要可包含云端缓存的 `remain_coins/checked_at/stale`；费用确认显示 RH 币和
  缓存状态，未知时只提示。工作台不解密 Key、不直接请求 `accountStatus`，也不记录完整响应。

## 2026-08-13 分批生成的人物图换图范围

- 新版工作台可按表格选中需要换图的脚本行，并将所选行锁定为本次换图范围，也可逐行加入
  或移出；该范围不改变已开始的声音、RunningHub、SeedVR2、后处理或变体任务。
- 后续上传新图片时，仅使用本次新上传的项目内部图片 ID，从目标范围第一行开始按当前
  count/loop 策略循环；例如 30 条中将第 11-30 条设为范围，新图只作用于后 20 条，前 10 条
  保持原图。存在表格勾选时，上传动作直接以当前勾选行覆盖换图范围；没有勾选时清除范围并
  保持原有全项目图片池分配逻辑。文件选择器返回的每个本地文件都创建新图片，不按名称或内容
  去重；局部换图的本批图片 ID 随映射设置持久化。
- 范围状态随项目持久化，范围内仍可单行版本化替换，空数组可清除范围。未新增数据库表或
  云端接口，也不会触发任何付费请求。
